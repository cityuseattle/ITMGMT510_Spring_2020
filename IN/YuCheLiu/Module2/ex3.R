x <- c(300.99, 1.6, 583, 42.10);
#floor function is to rounds to the largest integer but not greater than x
print(floor(x));
#ceiling function is to rounds to the smallest interger but not less than x
print(ceiling(x));
#only return the integer part of x
print(trunc(x));