#Create a vector with 7 elements including the length, sum, mean, median, var, standard, min and the max of the rivers vector.
v <- c(length(rivers), sum(rivers), mean(rivers), median(rivers), var(rivers), sd(rivers), min(rivers), max(rivers));
#print vector
print(v);