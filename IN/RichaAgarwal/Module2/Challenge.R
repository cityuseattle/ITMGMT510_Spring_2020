V <- c(length(rivers), sum(rivers), mean(rivers), median(rivers), var(rivers), sd(rivers),
      min(rivers), max(rivers))  
      #rivers is inbuilt data in R
      #length to count number of elements
      #mean to avg value of given elements
      #median mid value of given elements
      #sd as standard deviation to calcualte normal distribution between the data set-points
      #max and min  o find minimum and maximum value from given data set
      
      print(V)   # print the vector using V