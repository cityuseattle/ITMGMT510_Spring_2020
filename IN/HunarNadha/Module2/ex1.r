#Calculate the length of rivers
#Calculate the sum of a vector of rivers
#Calculate the mean of a vector of rivers
#Calculate the median of vector of rivers
#Calculate the var of vector of rivers
#Calculate the sd of vector of rivers
#Calculate the minimum of vector of rivers
#Calculate the maximum of vector of rivers
v <- c(length(rivers),sum(rivers),mean(rivers),median(rivers),var(rivers),sd(rivers),min(rivers),max(rivers));

print(v);
