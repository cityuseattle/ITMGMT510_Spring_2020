#Create a vector get rivers data
#Get the length (length) in rivers, and caculate their sum (sum), mean (mean), median (median), variance (var), standard deviation (sd), minimum (min) and maximum (max).
v <- c(length(rivers), sum(rivers), mean(rivers), median(rivers), var(rivers), sd(rivers), min(rivers), max(rivers));

print(rivers);

#The output is not the same as expected, I think the river database is different.