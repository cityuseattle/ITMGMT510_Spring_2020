#vector contains length, sum, mean, median, var, standard deviation, min and max of dataset "rivers"
v <- c(length(rivers), sum(rivers), mean(rivers), median(rivers), var(rivers), sd(rivers), min(rivers), max(rivers))

print(v)